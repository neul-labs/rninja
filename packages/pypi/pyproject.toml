[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rninja-cli"
version = "0.1.3"
description = "A drop-in replacement for Ninja build system with caching and improved scheduling"
readme = "README.md"
license = "MIT"
authors = [
    {name = "Neul Labs", email = "contact@neullabs.com"},
]
keywords = ["build", "ninja", "cache", "build-tool", "compiler"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Rust",
    "Topic :: Software Development :: Build Tools",
]
requires-python = ">=3.8"

[project.scripts]
rninja = "rninja.cli:main"
rninja-cached = "rninja.cli:cached_main"
rninja-daemon = "rninja.cli:daemon_main"

[project.urls]
Homepage = "https://rninja.neullabs.com"
Documentation = "https://docs.neullabs.com/rninja"
Repository = "https://github.com/neul-labs/rninja"
